export default {
  plugins: {
    // Tailwind CSS - génère les classes utilitaires
    tailwindcss: {},
    // Autoprefixer - ajoute les préfixes CSS pour la compatibilité navigateur
    // Exemple : transform devient -webkit-transform, -ms-transform, transform
    autoprefixer: {},
  },
}
